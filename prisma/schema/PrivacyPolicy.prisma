model PrivacyPolicy {
  id            String   @id @default("privacy-policy-singleton")
  title         String
  effectiveDate DateTime
  body          String   @db.Text
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  @@map("privacy_policy")
}
