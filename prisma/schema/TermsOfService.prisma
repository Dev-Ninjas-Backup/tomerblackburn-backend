model TermsOfService {
  id            String   @id @default("terms-of-service-singleton")
  title         String
  effectiveDate DateTime
  body          String   @db.Text
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  @@map("terms_of_service")
}
